# Pong

A small graphics + keyboard game built in the `uniClang` subset.

It demonstrates:
- `flag(0, 1)` / `flag(1, 1)` for graphics and keyboard
- `setpixel()` drawing
- `getkey()` polling
- `refreshscreen()` frame redraws
- fixed-width integer logic for position and collision checks
