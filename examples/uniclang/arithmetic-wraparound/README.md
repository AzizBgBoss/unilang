# Arithmetic wraparound

This example highlights the key rule in this language: unsigned arithmetic wraps modulo `2^N`.

It is useful for seeing how a narrow type like `uint8` behaves when it crosses its natural boundary.
