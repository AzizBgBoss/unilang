# Bitwise branches

This example demonstrates branch-heavy logic and bitwise operations in a compact style.

It covers:
- `if` / `else` chains
- `==`, `>`, `<`, `!=` comparisons
- bitwise `and`, `or`, and `xor`
- narrow unsigned behavior with masked values
